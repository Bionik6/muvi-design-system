import Foundation

enum Constants {
  // 🚨 red flag, the api key
  // should probably come from
  // the server and be stored in the
  // Keychain while enabling SSL-Pinning
  static let API_KEY = "c9856d0cb57c3f14bf75bdc6c063b8f3"
}
